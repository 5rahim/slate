import { Names } from './names'
import { Url } from './url'
import { Permissions } from './permissions'
import { Dates } from './date'

export const Utils = {
   Names,
   Permissions,
   Url,
   Dates,
}
